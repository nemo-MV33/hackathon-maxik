{
  "name": "hackathon-maxik",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --tls-max-v1.2 --watch src/index.js",
    "start": "node --tls-max-v1.2 src/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "@maxhub/max-bot-api": "^0.2.2",
    "dotenv": "^17.2.2"
  },
  "engines": {
    "node": ">=20"
  }
}
